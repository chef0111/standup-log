/**
 * Onboarding — first-time **Selected Repository** setup.
 * Route: `src/app/(app)/onboarding.tsx`
 *
 * UI is shared with settings via `@/features/repositories`.
 */
export { RepositoryPickerScreen } from '@/features/repositories';
